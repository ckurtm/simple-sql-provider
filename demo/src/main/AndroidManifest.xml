<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="ckm.simple.demo">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher">
        <provider
            android:authorities="@string/authority"
            android:name=".SimpleSQLProvider"/>
    </application>

</manifest>
